def incr_lista(l: lista): '''Devuelve una lista con elementos incrementados por 1'''
def es_primo(n): '''Devuelve verdadero si un numero es primo y falso al contrario'''
def anadir_elementos(arr, k): '''Dado un array de enteros no vacío arr y un entero k, devuelve la suma del primer elemento k que tenga al menos 2 dígitos'''
def solucion(lst): '''Dada una lista no vacía de enteros, devuelve la suma de todos los elementos impares que están en posiciones pares'''
def suma_arbol(root): '''Dada la raíz de un árbol, devuelve la suma de todos sus nodos'''
def incr_lista(l: lista): '''Devuelve una lista con elementos incrementados por 1'''
def es_primo(n): '''Devuelve verdadero si un numero es primo y falso al contrario'''
def anadir_elementos(arr, k): '''Dado un array de enteros no vacío arr y un entero k, devuelve la suma del primer elemento k que tenga al menos 2 dígitos'''
def solucion(lst): '''Dada una lista no vacía de enteros, devuelve la suma de todos los elementos impares que están en posiciones pares'''
def suma_arbol(root): '''Dada la raíz de un árbol, devuelve la suma de todos sus nodos'''
